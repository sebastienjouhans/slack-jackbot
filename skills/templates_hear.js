module.exports = function(controller) {

    // controller.hears(['^creative brief template$', '^where is the creative brief template$', '^give me the path creative brief template$'], 'direct_message,direct_mention', function(bot, message) {
    //     bot.reply(message, "path to creative brief template")
    // });

    // controller.hears(['^creative brief template$', '^where is the creative brief template$', '^give me the path creative brief template$'], 'direct_message,direct_mention', function(bot, message) {
    //     bot.reply(message, "path to creative brief template")
    // });

    // controller.hears(['^pitch template$', '^where is the pitch template$', '^give me the pitch template$'], 'direct_message,direct_mention', function(bot, message) {
    //     bot.reply(message, "path to pitch template")
    // });

    // controller.hears(['^presentation template$', '^where is the presentation template$', '^give me the presentation template$'], 'direct_message,direct_mention', function(bot, message) {
    //     bot.reply(message, "path to presentation template")
    // });

    // controller.hears(['^creds template$', '^where is the creds template$', '^give me the creds template$'], 'direct_message,direct_mention', function(bot, message) {
    //     bot.reply(message, "path to creds template")
    // });

};
